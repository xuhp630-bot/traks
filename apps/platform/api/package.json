{
  "name": "@traks/platform-api",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "doppler secrets download --no-file --format=env -p traks-api -c dev > .dev.vars.doppler && wrangler dev --port 5011 --persist-to ../../../.wrangler-state --env-file .dev.vars.doppler",
    "build": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:check": "drizzle-kit generate --name drift_check 2>&1 | tee /dev/stderr | grep -q 'No schema changes' || (git checkout -- src/db/migrations/ >/dev/null 2>&1; git clean -fd src/db/migrations/ >/dev/null 2>&1; echo '\\n✗ Schema drift: schema.ts has uncommitted changes. Run: yarn db:generate --name <descriptive_name>' && exit 1)",
    "db:migrate:dev": "wrangler d1 migrations apply traks-db-dev --local --persist-to ../../../.wrangler-state",
    "db:studio": "drizzle-kit studio",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts --fix",
    "format": "prettier --write src",
    "clean": "rimraf .turbo .wrangler .dev.vars.doppler"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.2",
    "@paralleldrive/cuid2": "^2.2.2",
    "@traks/shared": "*",
    "better-auth": "^1.6.25",
    "drizzle-orm": "^0.38.3",
    "hono": "^4.6.16",
    "ipaddr.js": "2.5.0",
    "robots-parser": "3.0.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260219.0",
    "@traks/eslint-config": "*",
    "drizzle-kit": "^0.30.1",
    "eslint": "^8.57.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.107.1"
  },
  "engines": {
    "node": ">=20"
  }
}
